---
name: <provider>-<action>
description: Battle-tested procedure for <action> on <provider>, distilled from real ship-loop deliveries. Routed below official installed skills and above generic implementers.
derived-from: <N> deliveries
providers: <provider>
last-verified: <YYYY-MM-DD>
success-rate: <passed>/<attempted>
---

<!-- PLAYBOOK RULES (delete this block in real playbooks):
  1. NO SECRETS, EVER. Procedures and template variables only ($VERCEL_TOKEN, $DOMAIN).
     Never values, account ids, project ids, or URLs containing tokens. Playbooks are
     built to be published; they must be secret-free by construction.
  2. Provenance frontmatter above is REQUIRED (validator-enforced for published
     playbooks). A draft's first live use must be flagged in the delivery handover.
  3. Every step carries its verification — a playbook is a procedure plus the evidence
     that proves each step worked.
-->

# <Provider>: <action>

## Preconditions
<!-- keys required (variable names only), CLI versions, repo state. -->

## Procedure
<!-- numbered, exact commands with $VARIABLES; each step followed by "Verify:" line. -->
1.
   Verify:

## Known failure modes
<!-- the gotchas that earned this playbook its existence; symptom → fix. -->

## Rollback
<!-- how to undo, or "irreversible — park behind human gate" -->
