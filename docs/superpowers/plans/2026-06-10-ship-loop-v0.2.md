# ship-loop v0.2 Implementation Plan (Iterate & Self-Evolution)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add `/ship:iterate`, the three-layer self-evolution subsystem (retrospective agent + user profile + playbook pipeline), and `needsSpecialist` escalation.

**Architecture:** Prompt-layer release — no ship-state.mjs changes. New assets: 1 command, 1 agent, 1 skill, 2 templates, playbooks/ dir spec; surgical edits to 4 existing skills + implementer agent; validator gains playbook provenance checks.

**Tech Stack:** unchanged (Node 22 tests + validator; markdown prompt assets).

**Source of truth:** spec `docs/superpowers/specs/2026-06-10-v0.2-iterate-evolution-design.md`. Tasks below carry REQUIRED invariants; full prose authored from the spec.

---

### Task 1: Profile + playbook templates, playbooks/ dir
**Files:** Create `templates/USER_PROFILE.template.md`, `templates/playbook.template.md`, `playbooks/README.md`.
- [ ] USER_PROFILE: sections for stack/taste/workflow/budget preferences each row carrying `evidence: <N products>`; proposals section; pointer to evolution.log append-only rule; local-overrides-global rule stated.
- [ ] playbook.template: SKILL.md shape with REQUIRED provenance frontmatter (`derived-from`, `providers`, `last-verified`, `success-rate`) + no-secrets rule banner + template-variable convention (`$VERCEL_TOKEN`).
- [ ] playbooks/README: lifecycle (consume → distill ≥2 deliveries → local draft → flagged first live use → human PR), routing priority line, provenance + no-secrets hard rules.
- [ ] Commit `feat: profile and playbook formats (v0.2 evolution layer)`.

### Task 2: Retrospective agent + skill; implementer escalation
**Files:** Create `agents/retrospect.md`, `skills/retrospect/SKILL.md`; Modify `agents/implementer.md`.
- [ ] retrospect agent frontmatter (tools: Read, Bash, Glob, Grep, Write; model inherit); reads ONLY existing traces (intake Q&A, loop-run-log, contracts/, verdicts, NEEDS_HUMAN, capability-map); never edits plugin files or product code.
- [ ] retrospect skill: trigger matrix (full after ship/iterate delivery, light after operate); the 5-row risk-tier output table from spec §4; ≥2-products promotion threshold; ≥2-deliveries playbook-draft threshold; evolution.log line format `<ISO-ts> | <class> | <change> | <evidence>`; structural ban on modifying skills/agents/templates.
- [ ] implementer.md: exit JSON gains optional `"needsSpecialist": "<category>"` + one rule line (request, don't hire — conductor re-routes; you still own the parked report).
- [ ] Commit `feat: retrospective engine + needsSpecialist escalation`.

### Task 3: Surgical edits to existing skills
**Files:** Modify `skills/design-intake/SKILL.md`, `skills/conductor/SKILL.md`, `skills/ship-deliver/SKILL.md`, `skills/capability-routing/SKILL.md`.
- [ ] design-intake: add **Path 2b — Iterate** (entry detection: frozen docs exist; feedback list as first-class input; reproduce-first via triage; delta questions only; `## Round N amendments`; re-freeze; prior tests = regression floor) + L2 prefill rule (read `~/.claude/ship-loop/profile/` first, confirm instead of cold-ask; project answer overrides profile).
- [ ] conductor: read charter-defaults overlay at entry; verdict handling gains `needsSpecialist` re-route branch (next round, via routing-overrides); after ship-deliver completes → dispatch retrospect (full).
- [ ] ship-deliver: keys-scan execution consults playbooks first (priority: official skill > verified playbook > flagged draft > generic); delivery evidence written so retro can distill; final step dispatches retrospect.
- [ ] capability-routing: priority tiers incl. local playbooks dir scan (`~/.claude/ship-loop/playbooks/`); read routing-overrides.md; record hit→attempts stats line into capability-map for retro.
- [ ] Commit `feat: wire evolution layer into intake, conductor, deliver, routing`.

### Task 4: `/ship:iterate` command
**Files:** Create `commands/ship-iterate.md`.
- [ ] Frontmatter description + argument-hint `"[feedback.md | pasted complaints] "`; refuses when ACTIVE exists; flow: triage reproduce → design-intake Path 2b → re-freeze → ACTIVE → conductor; states operate/iterate/ship boundary one-liner.
- [ ] Commit `feat: /ship:iterate re-entry command`.

### Task 5: Validator + docs + version
**Files:** Modify `scripts/validate-repo.mjs`, `README.md`, `README.zh-CN.md`, `.claude-plugin/plugin.json`, `STATE.md`, `docs/architecture.md`.
- [ ] validate-repo: playbooks/*/SKILL.md (when present) must carry the 4 provenance frontmatter fields; commands/ scan now includes ship-iterate cross-refs.
- [ ] README: command table + "Self-evolution" section (3 layers, proposal-first, playbook pipeline + commercialization paragraph); zh mirror; architecture.md gains the layer diagram row.
- [ ] plugin.json → 0.2.0.
- [ ] Commit `docs+chore: v0.2.0 (iterate + self-evolution)`.

### Task 6: Verify + publish
- [ ] `node --test` (13 pass), `node scripts/validate-repo.mjs` (pass), `bash -n` scripts.
- [ ] Push, tag `v0.2.0`, confirm CI green.

## Self-review
Spec coverage: §2→T1/T2/T3; §3→T3(design-intake)/T4; §4→T2; §5→T1/T3(deliver,routing)/T5(validator); §6→T2(implementer)/T3(conductor); §7 honored (no engine changes). Names used consistently: `~/.claude/ship-loop/profile/`, `proposals.md`, `evolution.log`, `routing-overrides.md`, `playbooks/`, `needsSpecialist`, `Round N amendments`.
